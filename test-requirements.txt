# Master Test Requirements
# Install this before running tests: pip install -r test-requirements.txt

# Core testing frameworks
pytest==9.0.1
pytest-asyncio==1.3.0
pytest-mock==3.15.1
pytest-cov==7.0.0

# FastAPI testing
httpx==0.27.2
fastapi==0.115.4
pydantic==2.9.2

# AWS services
boto3==1.35.0

# Environment configuration
python-dotenv==1.0.1

# Google AI (for classification tests)
google-generativeai==0.8.3

# Data processing (for risk prediction)
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2
scikit-learn==1.5.2

# NOTE: Heavy dependencies excluded from tests (mocked instead):
# - chromadb (200+ MB, slow to install)
# - langchain (not needed for unit tests)
# - sentence-transformers (ML model, heavy)
# - xgboost (ML model, heavy)
